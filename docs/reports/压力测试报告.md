# AI Performance Engineer — 压测报告

| 字段 | 值 |
|------|-----|
| **压测日期** | 2026-07-26 |
| **压测工具** | curl + bash (原生, 无依赖) |
| **总请求数** | 250 |
| **成功率** | 99.6% (249/250) |
| **失败数** | 1 |

---

## 1. 压测目标

**被压测服务**: StressDemo (Spring Boot 2.7.18 + JDK 8)

**访问地址**: `http://104.207.143.198:8090`

**核心接口**:

| 接口 | 用途 | 瓶颈类型 |
|------|------|---------|
| GET /api/products/{id} | 单条查询 (命中索引) | DB 读 |
| GET /api/products?page=1&size=100 | 分页查询 (全表扫描) | DB 读 + 网络 |
| GET /api/products/search?keyword= | LIKE 全表扫描 | DB 读密集 |
| GET /api/compute/fibonacci/{n} | 递归斐波那契 | CPU 密集 |
| GET /api/compute/prime/{n} | 素数计算 | CPU 密集 |
| GET /api/slow?delay=2000 | 可控延迟 | RT 飙高 |
| GET /api/memory/leak?mb=10 | 内存泄漏模拟 | 堆内存压力 |
| GET /api/cache/{key} | 缓存 hit/miss | 内存/CPU |
| GET /api/stats | 系统状态 | 监控 |

---

## 2. 部署环境

### 2.1 服务器拓扑

```
┌─────────────────────────────────────────────────────────────┐
│  Host A (Platform) — 104.207.143.198 (Ubuntu 22.04, 4C4G) │
│  ┌─ aipe-resource      :8082  ← 统一资源抽象               │
│  ├─ aipe-observation   :8083  ← 运行时构建+CH落地           │
│  ├─ aipe-relationship  :8084  ← 关系管理+拓扑投影           │
│  ├─ aipe-timeline      :8085  ← 运行时统计特征+趋势检测     │
│  ├─ aipe-evidence      :8086  ← 异常检测+因果推理           │
│  ├─ aipe-knowledge     :8087  ← 知识沉淀+版本管理           │
│  ├─ aipe-recommendation :8088 ← 知识→建议+优先级            │
│  ├─ aipe-execution     :8089  ← 执行+效果评估+闭环          │
│  └─ stress-demo        :8090  ← 压测靶机                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Host B (Database) — 124.223.220.245 (CentOS 7, 4C4G)     │
│  ├─ MySQL 8.0.33      :3306  ← 元数据/知识/执行 (aipe_metadata) │
│  └─ ClickHouse 23.8   :8123  ← 时序/事实 (metric_observation) │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 组件 | 版本 | 说明 |
|------|------|------|
| JDK | 1.8.0_491 | 全项目统一 Java 8 |
| Spring Boot | 2.7.18 | 全模块统一 |
| MyBatis Plus | 3.5.3.1 | ORM (MySQL) |
| Druid | 1.2.20 | 连接池 (MySQL) |
| ClickHouse JDBC | 0.4.6 | 时序查询 (HTTP 模式) |
| Tomcat JDBC Pool | 9.0.83 | ClickHouse 专用池 |

---

## 3. 压测执行过程

### 3.1 阶段1: 基线测试 (10 请求)

```
GET /api/products/1 × 10
目的: 确认服务可用, 建立响应时间基线
```

### 3.2 阶段2: 轻负载 (50 请求)

```
GET /api/products/1 × 50
目的: 模拟常规查询负载
```

### 3.3 阶段3: CPU 密集 (30 请求)

```
GET /api/compute/fibonacci/35 × 30
目的: 压测 CPU 密集型接口, 观察响应时间劣化
```

### 3.4 阶段4: 慢响应 (20 请求)

```
GET /api/slow?delay=2000 × 20
目的: 模拟慢查询/下游超时场景
```

### 3.5 阶段5: 缓存场景 (50 请求)

```
GET /api/cache/key-{0..9} × 50
目的: 验证缓存命中/未命中性能
```

### 3.6 阶段6: 混合场景 (90 请求, 并发 30)

```
并发请求:
  GET /api/products/1    × 30
  GET /api/compute/prime/3000 × 30
  GET /api/stats         × 30
目的: 模拟真实混合负载
```

---

## 4. 压测结果

### 4.1 总体统计

| 指标 | 值 |
|------|-----|
| **总请求数** | 250 |
| **成功 (HTTP 200)** | 249 |
| **失败** | 1 |
| **成功率** | 99.6% |

### 4.2 响应时间分布

| 指标 | 值 (ms) |
|------|---------|
| **最小** | 470 |
| **最大** | 11,131 |
| **平均** | 1,195 |
| **P50 (中位数)** | 921 |
| **P95** | 2,709 |
| **P99** | 6,244 |

### 4.3 分析

1. **P50 = 921ms**: 一般偏慢, 原因是 4G 内存同时跑 8 个平台模块 + 压测靶机 + 大量 GC
2. **P99 = 6244ms**: 长尾请求主要由 CPU 密集 (fibonacci/prime) + 慢响应 (sleep 2s) 导致
3. **1 次失败**: 可能是并发高峰期间的瞬时连接拒绝 (连接池耗尽)
4. **成功率 99.6%**: 平台整体稳定

### 4.4 瓶颈定位

| 瓶颈 | 表现 | 原因 |
|------|------|------|
| 内存不足 | 响应慢, GC 频繁 | 4GB 同时跑 9 个 JVM + 2 个 DB |
| CPU 争用 | fibonacci/prime 响应 >3s | 30 并发 CPU 密集任务 |
| 连接池 | 1 次失败 | 并发超过 maxActive (10) |

---

## 5. AI 平台观测验证

压测过程中, AI Performance Engineer 平台自动采集并处理了压测产生的指标:

### 5.1 Observation 落库 (ClickHouse)

```sql
SELECT COUNT(*) FROM metric_observation.observation_fact;
-- 结果: 75+ 条 (压测期间自动采集)
```

### 5.2 Timeline 构建 (运行时)

```
GET /api/v1/timelines?resource_id=f1f7d51d-...&metric_name=cpu.usage&start_time=...&end_time=...
返回: {timelineId, pointCount, stats: {min, max, avg, stdDev}}
示例: pointCount=13, min=86.0, max=99.0, avg=92.7
```

### 5.3 Evidence 推理 (规则引擎)

```
GET /api/v1/evidences?resource_id=...
触发: CPU 持续 >80% → 自动生成 Performance Evidence
置信度: 0~100% (基于超阈值程度 + 持续时间)
```

### 5.4 Knowledge 沉淀 (经验库)

```
POST /api/v1/knowledge
→ 创建 Knowledge (type=BOTTLENECK, confidence=85, successRate=75)
→ 未来同类问题可直接引用
```

### 5.5 Recommendation 生成

```
POST /api/v1/recommendations/generate
→ Knowledge → Recommendation (priority=HIGH/MEDIUM/LOW)
→ 附带 execution_plan + rollback_plan
```

### 5.6 Execution 执行

```
POST /api/v1/executions
→ 创建 Execution (status=PENDING→EXECUTED)
→ 触发 Optimization 评估 (before/after 对比)
→ Knowledge successRate 更新 (闭环)
```

---

## 6. 结论

✅ **平台功能完整**: Resource → Observation → Timeline → Evidence → Knowledge → Recommendation → Execution 全流程跑通

✅ **压测通过**: 250 请求, 99.6% 成功率, 平均响应 ~1.2s

⚠️ **性能瓶颈**: 4G 内存限制, 建议生产环境:
- 平台模块单独部署 (≥8G)
- 数据库单独部署 (≥8G)
- ClickHouse 使用独立 SSD

---

*本报告由 AI Performance Engineer 自动生成*

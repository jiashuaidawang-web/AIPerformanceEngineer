# ============================================================
# AI Performance Engineer - Agent Configuration (VM Deploy)
# ============================================================

agent:
  agentId: agent-001
  serverId: server-001
  environment: dev
  backendUrl: http://108.160.128.34:8081
  schedulerPoolSize: 4
  sendTimeoutMs: 5000
  connectors:
    - type: JVM
      enabled: true
      intervalMs: 30000
      properties:
        jvmType: hotspot

    - type: LINUX
      enabled: true
      intervalMs: 30000
      properties:
        procPath: /proc

    - type: REDIS
      enabled: true
      intervalMs: 30000
      properties:
        host: 124.223.220.245
        port: "6379"
        password: "pamirs@123"

    - type: MYSQL
      enabled: true
      intervalMs: 30000
      properties:
        host: 124.223.220.245
        port: "3306"
        user: root
        password: astock_root
        database: aipe_metadata
